{% extends "baseBackend.html.twig" %}

{% block title %}{{ '%entity% creation'|trans({'%entity%': 'Reclamation'}, 'admin') }} - {{ parent() }}{% endblock %}

{% block content %}
    <h1>{{ '%entity% creation'|trans({'%entity%': 'Reclamation'}, 'admin') }}</h1>

    {{ form_start(form, {action: path('reclamation_create')}) }}
        {{ form_widget(form) }}
        <p>
            <button class="btn btn-primary" type="submit">{{ 'Create'|trans({}, 'admin') }}</button>
        </p>
    {{ form_end(form) }}

        
        <div class="btn-group record_actions">

            <a class="btn btn-info" href="{{ path('reclamation') }}">
                <i class="fa fa-list"></i> {{ 'Back to the list'|trans({}, 'admin') }}
            </a>


        </div>


{% endblock %}
